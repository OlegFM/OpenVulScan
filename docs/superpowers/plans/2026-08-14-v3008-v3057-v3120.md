# V3008 + V3057 + V3120 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Three definite-only rules — V3008 (assigned twice successively), V3057 (definitely negative argument), V3120 (potentially infinite loop).

**Architecture:** V3008 = AstRule over the method CFG, intra-block statement scan (V3151 precedent). V3057 = DataFlowRule on the interval SSA pipeline (V3106 precedent). V3120 = AstRule on while/do/for syntax with a conservative purity check.

**Tech Stack:** .NET 10, Roslyn syntax + IOperation/CFG, Verify snapshots.

**Spec:** `docs/superpowers/specs/2026-08-14-v3008-v3057-v3120-design.md`

## Global Constraints

- `TreatWarningsAsErrors=true`; CA1707 in Rules.Tests (no underscores in test method names).
- Flat namespace `OpenVulScan`. Rule levels/CWE from RulesMap.xml: V3008 = Level2/CWE-563, V3057 = Level2/CWE-628, V3120 = Level1/CWE-835.
- Snapshot flow: run test → `.received.txt` appears → inspect → rename to `.verified.txt` → rerun green. Snapshots live next to the test file.
- Commits: `git -c user.name="Oleg" -c user.email="olegefm@gmail.com" commit …` + trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.

---

### Task 1: V3008 (assigned twice successively)

**Files:**
- Create: `src/OpenVulScan.Rules.Ast/V3008DoubleAssignment.cs`
- Test: `tests/OpenVulScan.Rules.Tests/V3008Tests.cs` (+ `.verified.txt` snapshots)

**Interfaces:**
- Consumes: `AstRule.OnMethodDeclaration(SyntaxNodeContext)`, `ControlFlowGraph.Create(IMethodBodyOperation)`, `block.Operations`.
- Produces: rule class `V3008DoubleAssignment` (auto-discovered by registry scan).

- [ ] **Step 1: Write snapshot tests** — cases: `TwoSuccessiveAssignmentsDetected` (`int x = 1; x = 2;` → 1 hit at the second), `ThreeAssignmentsDetectedTwice` (`x=1; x=2; x=3;` → 2 hits), `ReadBetweenAssignmentsNotFlagged` (`x=1; y=x; x=2;`), `SelfReferencingSecondAssignmentNotFlagged` (`x=1; x=x+1;`), `BranchBetweenAssignmentsNotFlagged` (`x=1; if(c){} x=2;`), `RefArgumentBetweenNotFlagged` (`x=1; F(ref x); x=2;`), `CompoundAssignmentNotFlagged` (`x=1; x+=2;`).
- [ ] **Step 2: Run, verify RED** (missing `.verified` / empty diagnostics where hits expected).
- [ ] **Step 3: Implement** — per CFG block: `Dictionary<ISymbol, IOperation> pending`; for each statement op (unwrap `IExpressionStatementOperation`): simple assignment to `ILocalReferenceOperation` target → clear pending for every local referenced in the VALUE subtree, then report at this op if `pending` holds the target, then `pending[target] = op`; declarator with initializer → same; anything else → clear pending for every local referenced anywhere in the statement (covers compound targets, `++`, ref/out args). Pending resets per block.
- [ ] **Step 4: Inspect `.received`, accept `.verified`, rerun GREEN.**
- [ ] **Step 5: Commit** `feat(rules): V3008 successive assignment dead store (#ovs-2qi.16)`.

### Task 2: V3120 (potentially infinite loop)

**Files:**
- Create: `src/OpenVulScan.Rules.Ast/V3120InfiniteLoop.cs`
- Test: `tests/OpenVulScan.Rules.Tests/V3120Tests.cs` (+ snapshots)

- [ ] **Step 1: Write snapshot tests** — `WhileConditionVariableNeverChangedDetected` (`int i=0; while(i<10){ n=n+1; }` → hit), `DoWhileNeverChangedDetected`, `ForWithoutIncrementorDetected` (`for(; i<10;){ }`), `MutatedInBodyNotFlagged` (`while(i<10){ i=i+1; }`), `BreakInBodyNotFlagged`, `ReturnInBodyNotFlagged`, `RefEscapeNotFlagged` (`while(i<10){ F(ref i); }`), `ConditionWithCallNotFlagged` (`while(Check(i)){}`).
- [ ] **Step 2: RED.**
- [ ] **Step 3: Implement** — hooks `OnWhileStatement`/`OnDoStatement`/`OnForStatement`. `TryCollectConditionLocals`: allow identifier/literal/binary/prefix-unary/paren/cast nodes only, resolve identifiers to `ILocalSymbol`/`IParameterSymbol` (else bail); empty set ⇒ skip. Body scan (`DescendantNodes`, plus for-incrementors as mutation scope): bail-to-silent on any assignment/`++`/`--`/ref/out arg targeting a condition var, or any lambda/local function referencing one; bail on `break`/`return`/`goto`/`throw`/`yield`. Otherwise report at the condition.
- [ ] **Step 4: Accept snapshots, GREEN.**
- [ ] **Step 5: Commit** `feat(rules): V3120 potentially infinite loop (#ovs-2qi.16)`.

### Task 3: V3057 (definitely negative argument)

**Files:**
- Create: `src/OpenVulScan.Rules.DataFlow/V3057OddArgument.cs`
- Test: `tests/OpenVulScan.Rules.Tests/V3057Tests.cs` (+ snapshots)

**Interfaces:** interval pipeline from ovs-kmj — `IntervalSsaEvaluator.Evaluate(op, state, context.SsaIndex)`, lattice/transfer/refiner identical to `V3106IndexOutOfBounds`.

- [ ] **Step 1: Write snapshot tests** — `NegativeSubstringStartDetected` (`s.Substring(-1)`), `GuardProvenNegativeDetected` (`if (i < 0) s.Substring(i)`), `NegativeArraySizeDetected` (`new int[n]` with `n = -5`), `NonNegativeNotFlagged` (`s.Substring(0)`), `UnknownArgumentNotFlagged` (param), `NegativeRemoveDetected` (`s.Remove(-2)`).
- [ ] **Step 2: RED.**
- [ ] **Step 3: Implement** — `DataFlowRule<ImmutableDictionary<SsaId, IntervalValue>>`; OnState: string instance invocation with name in `{Substring, Remove, PadLeft, PadRight}` → each `int`-typed argument with `!interval.IsEmpty && interval.Upper < 0` reported; `IArrayCreationOperation` (rank 1) with size interval `Upper < 0` reported. Message: `"'{0}' receives a definitely negative value {1}"`.
- [ ] **Step 4: Accept snapshots, GREEN; run FULL suite** (registry picks all three rules — corpus may shift; inspect any change).
- [ ] **Step 5: Commit** `feat(rules): V3057 definitely negative argument via intervals (#ovs-2qi.16)`.

### Task 4: Close-out

- [ ] Commit docs (spec+plan), `bd close ovs-2qi.16`, file follow-ups (V3008 cross-block, V3057 MAY/API breadth, V3120 fields), `bd export --all -o .beads/issues.jsonl`, commit, `git pull --rebase && bd dolt push && git push`, verify up-to-date.
