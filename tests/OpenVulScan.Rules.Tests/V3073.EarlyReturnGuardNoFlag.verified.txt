{
  RuleCode: V3073,
  TestCase: EarlyReturnGuardNoFlag,
  Source:
sealed class Res : System.IDisposable { public void Dispose() {} }
class C : System.IDisposable {
    private Res _a = new Res();
    public void Dispose() {
        if (_a == null) { return; }
        _a.Dispose();
    }
}
}