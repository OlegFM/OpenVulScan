{
  RuleCode: V3114,
  TestCase: TryFinallyNoFlag,
  Source:
sealed class Res : System.IDisposable { public void Dispose() {} public void Use() {} }
class C {
    void M() {
        var r = new Res();
        try { r.Use(); }
        finally { r.Dispose(); }
    }
}
}